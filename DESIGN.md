# DESIGN.md — 标讯截图助手

> 状态：Baseline v0.1  
> 更新日期：2026-07-20  
> 设计目标：让用户在 30 秒内完成批量建任务，并能在一个矩阵页面判断九个平台的执行结果和证据完整性。

## 1. 产品体验原则

### 1.1 证据比“成功”更重要

界面不只显示绿色成功。每个成功状态必须能展开查看：命中公告、原始地址、采集时间、截图/快照、文件哈希和执行步骤。

### 1.2 状态必须可行动

失败提示必须回答三个问题：

1. 卡在哪个平台和步骤；
2. 是未查询到、页面变化、登录失效、验证码、超时还是邮件失败；
3. 用户下一步可以重试、人工接管还是更新 Adapter。

### 1.3 批量任务仍然可追踪

任务、查询名称、平台执行项是三级对象。批量输入后，用户仍能定位到某个项目在某个平台的单次运行。

### 1.4 自动化不掩盖风险

需要人工验证时明确暂停，不通过反复点击或技术规避伪装成自动完成。

## 2. 信息架构

```text
标讯截图助手
├── 任务
│   ├── 任务列表
│   ├── 新增任务
│   └── 任务详情
├── 执行中心
│   ├── 平台矩阵
│   ├── 失败项
│   └── 人工接管
├── 证据库
│   ├── 公告记录
│   ├── 文件与哈希
│   └── 归档包
├── 平台管理
│   ├── Adapter 状态
│   ├── 登录状态
│   └── 回归检查
└── 系统设置
    ├── 邮件与渠道
    ├── 并发与重试
    └── 审计与保留策略
```

## 3. 核心页面

### 3.1 任务列表

主列：任务名称、查询名称数、平台数、进度、命中、成功、失败、耗时、邮件状态、创建时间。

批量操作：立即执行、仅重试失败项、修改收件人、下载归档、复制任务、删除。

筛选：名称模糊搜索、任务状态、平台状态、创建时间、是否有人工接管项。

### 3.2 新增任务

表单分为四段：

1. 基本信息：任务名称、收件邮箱；
2. 查询对象：批量文本/Excel、智能识别预览、去重；
3. 查询规则：平台、时间范围、公告类型、匹配方式、包含词、排除词；
4. 执行设置：立即/定时、截图范围、命中上限、失败重试。

批量文本框必须提供：识别数量、重复数量、被清理的序号、最终名称预览。

### 3.3 执行矩阵

行是查询名称，列是九个平台。单元格状态：

- `已命中`
- `未查询到`
- `部分命中`
- `截图成功`
- `需要登录`
- `需要验证码`
- `页面结构变化`
- `执行超时`
- `平台异常`

点击单元格打开右侧抽屉：执行时间线、命中列表、证据预览、反馈、重试与人工接管。

### 3.4 证据详情

证据预览区同时展示：

- 页面快照；
- 页面标题和 URL；
- 采集时间与时区；
- SHA-256；
- Task ID、Run ID、Query ID、Platform ID；
- 下载原文件和查看 manifest。

## 4. 视觉系统

### 4.1 风格

企业工具型、清晰、克制、高密度但不拥挤。避免营销式大卡片和无意义渐变。

### 4.2 设计令牌

```css
:root {
  --bg-canvas: #f5f7fa;
  --bg-surface: #ffffff;
  --text-primary: #172033;
  --text-secondary: #5b667a;
  --border-default: #dce2ea;
  --brand-primary: #2457d6;
  --brand-primary-hover: #1948bd;
  --status-success: #16845b;
  --status-warning: #b26a00;
  --status-danger: #c23934;
  --status-info: #2563a6;
  --radius-sm: 6px;
  --radius-md: 10px;
  --shadow-panel: 0 6px 20px rgba(23, 32, 51, 0.08);
}
```

### 4.3 字体与尺寸

- 系统字体栈：`Inter, PingFang SC, Microsoft YaHei, sans-serif`；
- 页面标题 24/32，区块标题 18/26，正文 14/22，辅助文字 12/18；
- 表格默认 14px，紧凑模式 13px；
- 交互控件最小高度 36px；
- 状态不只依赖颜色，必须同时显示文字或图标。

## 5. 交互约束

- 删除、批量删除、覆盖已有归档需要二次确认；
- “重新执行”默认只执行失败或过期项，完整重跑需显式选择；
- 点击发送邮件前显示收件人、附件大小、成功/失败项摘要；
- 遇到验证码时保留当前浏览器会话与执行上下文；
- 页面改版失败不得自动把 `NOT_FOUND` 当作成功。

## 6. 响应式与无障碍

- 桌面端优先，核心断点：1440、1280、1024；
- 1024 以下矩阵横向滚动，冻结查询名称列；
- 键盘可访问表单、抽屉和表格单元格；
- 对比度满足 WCAG AA；
- 运行状态变化使用 `aria-live` 提示，不持续刷屏。

## 7. 当前实现映射

当前 FastAPI 根页面是最小交互原型，只验证信息结构、设计令牌和 API 连通性。正式 React 前端将在任务 API、运行模型和 Adapter 状态稳定后实现，避免先做静态大屏再返工数据契约。
