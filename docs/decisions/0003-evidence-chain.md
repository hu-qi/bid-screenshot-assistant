# ADR-0003：成功结果必须绑定证据链

- 状态：Accepted
- 日期：2026-07-20

## Context

仅返回“成功/未命中”无法证明实际查询过程，也难以区分无结果与技术故障。

## Decision

每个平台执行项必须生成结构化结果和证据文件。文件写入后计算 SHA-256，并进入 manifest。`NOT_FOUND` 也必须保存完成搜索后的无结果页面证据。

## Consequences

- 审阅和追责更清晰；
- 存储量增加，需要保留策略；
- Artifact 写入失败会影响执行项最终状态，不能忽略。
