# 广东政府采购智慧云平台安全边界

- 仅访问公开门户和公开检索/详情接口；
- 不登录电子卖场、采购交易工作台或供应商系统；
- 不使用账号、Cookie、Token 或未公开接口；
- 只允许 `gdgpo.czt.gd.gov.cn` HTTPS URL；
- 门户路由 403 不尝试绕过，返回 `PARTIAL`；
- 附件当前只记录同域 URL，不自动下载；
- 后续下载需限制 MIME、扩展名、大小和重定向域名；
- API 错误不解释为 `NOT_FOUND`；
- 不执行报名、投标、报价、付款、异议或合同操作。
